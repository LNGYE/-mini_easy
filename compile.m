main()
{
    # 声明
    int a,b,c,d;
    int w[4];
    int *p,*q;

    # 指针赋值
    p=&a;  # ptr_upper=0
    q=w;   # ptr_upper=3

    # TAC_COPY
    a=1;
    b=a;
    w[0]=1;
    w[1]=w[0];
    c=w[0];
    d=*p;
    w[2]=*q;
    w[3]=*(q+1);

    *p=2;
    *p=b;
    *p=w[1];
    *p=*q;
    *p=*(q+1);

    w[0]=0;
    w[1]=1;
    w[2]=2;
    w[3]=3;

    *(q+1)=*q;
    *(q+2)=*(q+1);
    *(q+3)=*(q+2);


    # TAC_ADD
    a=b+c;
    b=c+w[0];
    c=w[1]+w[2];
    d=*p+w[0];
    a=*p+*q;
    b=*(q+1)+w[2];
    c=*(q+2)+*(q+3);

    w[0]=1+a;
    w[0]=w[1]+w[2];
    w[0]=w[1]+b;
    w[1]=*p+*q;
    w[1]=*p+*(q+2);
    w[1]=*(q+2)+*(q+3);

    *p=1+a;
    *p=w[1]+w[2];
    *p=w[1]+b;
    *p=*q+1;
    *p=*(q+2)+*(q+3);

    *(q+1)=1+a;
    *(q+1)=w[0]+w[2];
    *(q+1)=w[0]+b;
    *(q+1)=*q+1;
    *(q+1)=*(q+2)+*(q+3);
}